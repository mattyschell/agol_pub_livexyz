To run:

1. Replace `your_token_here` with the Api Key found in the (directory)[https://directory.livexyz.com/] top right drop down menu
2. Add desired logic in `example.py` while loop where it reads `#Do something with the data here` 
3. Run from CLI `python example.py`